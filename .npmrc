# pnpm v9 — approve build scripts for packages that require native compilation
# Required for: Next.js image optimization (sharp), Firebase (protobufjs, @firebase/util), MSW (msw)
onlyBuiltDependencies=@firebase/util,msw,protobufjs,sharp
